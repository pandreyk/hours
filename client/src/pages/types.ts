export enum AdminRoutesList {
  home = '/home',
  assignment = '/assignment',
  teachers = '/teachers',
  classes = '/classes',
  classSchedule = '/classes/:classId(\\d+)/schedule',
  lessons = '/lessons',
  pupils = '/pupils',
  subjects = '/subjects',
}
